# QLK Authenticator For Odoo10
Two Factor authentication to authenticate user via OTP.

-------------------------

To Install The external Dependencies, type in your terminal :

            > pip install pyotp
            > pip install qrcode
